过程式程序设计:确定需要哪些过程，采用能找到的最好的算法。
模块程序设计:确定需要哪些模块，将程序分为一些模块，将数据隐藏在模块支中。  --数据隐藏原理
用户定义类型:确定你需要哪些类型，为每个类型提供完整的一组操作。
面向对象程序设计:确定需要哪些类，为每个类提供完整的一组操作，利用继承去明确表示共性。
通用型程序设计：确定需要哪些算法，将它们参数化，使它们能够对各种各样的类型和数据结构工作。